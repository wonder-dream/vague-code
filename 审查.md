# XClaw 项目审查报告

> 审查对象：`D:\document\xcode`（Coding Agent CLI/TUI，Python 3.12）
> 审查方式：全量源码阅读（`src/` 9,317 行 + `tests/` 13,984 行 + `eval/` + `docs/`）+ 全量测试实跑 + git 历史核对
> 测试验证：`pytest -q --ignore=tests/_target_bug` → **759 passed, 13 warnings in 161.87s**（实跑通过）

---

## 目录

1. [项目概览](#1-项目概览)
2. [数据流动链路](#2-数据流动链路)
3. [优点](#3-优点)
4. [缺点与风险](#4-缺点与风险)
5. [改进建议（按优先级）](#5-改进建议按优先级)
6. [总结](#6-总结)

---

## 1. 项目概览

XClaw 是一个**面向真实编码场景的本地 Coding Agent**，具备完整闭环：Agent Runtime（ReAct 循环）→ 工具系统（7 工具 + 冲突可串行化并发）→ 上下文工程（五层压缩）→ 权限安全 → 跨会话记忆 → Repo Map 符号索引 → 模型抽象层（自定义 IR + 双厂商 codec）→ 事件溯源轨迹 → CLI/TUI 双前端 → SWE-bench 评测工具链。

| 维度 | 数据 |
|---|---|
| 源码规模 | `src/` 9,317 行，`eval/` ~4,000 行，`tests/` 13,984 行 |
| 测试 | 53 个测试文件 / 759 个用例，全量通过（2 分 41 秒） |
| 文档 | 20 份 ADR + 18 份 plans + 25 篇文章 + 9 份 handoff + CHANGELOG |
| 评测任务集 | `eval/tasks.json` 20 题（SWE-bench Verified 官方保留，本机可跑） |
| 后端 | DeepSeek（OpenAI 兼容）/ Anthropic 双 codec，统一 IR |
| 前端 | CLI（argparse + Rich）+ TUI（Textual，v2 分层架构） |
| 工程质量 | ruff + mypy 零错误，GitHub Actions CI，759 测试通过 |

---

## 2. 数据流动链路

### 2.1 总览

```
┌─────────────────────────── 入口层 ───────────────────────────┐
│  CLI (src/cli) │ TUI (src/tui) │ Eval Harness (eval/cli)    │
└──────────────┬──────────────────────────┬───────────────────┘
               │ task/workdir             │ tasks.json → matrix
               ▼                          ▼
┌──────────────────────────────────────────────────────────────┐
│                    Agent Runtime (src/agent/loop.py)          │
│  _init_run: RepoIndex → SystemPrompt → 工具绑定 → run_start 事件│
│  _run_gen:  ReAct 主循环（见 2.2 详图）                        │
│  监督 Agent: 周期/终局五值评估 → guidance 注入 → stuck 判停     │
└──────┬──────────────────┬──────────────────┬─────────────────┘
       │                  │                  │
       ▼                  ▼                  ▼
┌────────────┐   ┌───────────────┐   ┌──────────────────┐
│ Model Layer │   │ Tool System   │   │ Trajectory       │
│ backend.py  │   │ tools.py      │   │ trajectory.py    │
│ codecs/*    │   │ concurrency   │   │ JSONL → SQLite   │
│ StreamEvent │   │ permission    │   │ to_messages()    │
└────────────┘   └───────────────┘   └────────┬─────────┘
                                              │ 离线消费
                                              ▼
                              Eval: verify(F2P/P2P) / metrics /
                                   judge(LLM) / classify / report
```

### 2.2 Agent 主循环（ReAct）链路

```
用户 task
  │
  ▼
_init_run(task, workdir)
  ├─ RepoIndex.build()（tree-sitter 符号索引）→ repo_map_text（≤1000 token 注入 system）
  ├─ SystemPrompt.build()（身份 + .agent/rules.md 层级规则 + 工作目录）
  ├─ bound_tools = {name: tool.factory(workdir)}（路径穿越防护在工厂内）
  ├─ memory_store 初始化 → 注册 memory_search 工具
  └─ traj.emit(run_start) ──→ Trajectory.events
  │
  ▼
_run_gen 每轮（turn_box[0] < max_turns）：
  ├─ 周期监督（每 period 轮）→ guidance 注入为 user 消息
  ├─ compress_chain（五层压缩，见 2.3）→ 记录 compression 事件
  ├─ backend.stream(messages, tools) ──→ codec.encode_request（IR→wire）
  │      │
  │      ▼
  │   SSE chunks → DeepSeek/Anthropic StreamDecoder ──→ StreamEvent 序列
  │      （MessageStart/Thinking*/TextDelta/ToolUseStart/ArgsDelta/ToolUseEnd/MessageEnd/RetryNotice）
  │      │
  │      ├─ yield 给前端（CLI Rich visitor / TUI _on_stream_event）实时渲染
  │      ├─ _StreamAggregator.feed(ev) 聚合 → ModelResponse（IR）
  │      └─ 异常 → classify_llm_error → RetryPolicy（指数退避）→ RetryNotice
  │
  ├─ stop_reason == tool_use：
  │     ├─ 权限预检（_check_tool_permission：模式决策 + 持久规则 + prewrite diff 审查）
  │     │     ├─ DENY → ToolResultBlock(is_error) 回填
  │     │     └─ ALLOW → 串行 or execute_concurrent（冲突可串行化分组）
  │     ├─ 工具执行（read/write/patch/glob/grep/bash/code_search/memory_search）
  │     │     └─ 结果 50K 字符截断 → ToolResultBlock
  │     ├─ messages.append(assistant) + append(user tool_results)
  │     └─ checkpoint（traj.persist）→ 下一轮
  ├─ stop_reason == end_turn：
  │     ├─ 终局监督判定（done → supervisor_done 终止 / 其他 → 继续）
  │     └─ run_end → 终止
  └─ max_tokens/content_filter → run_end → 终止
  │
  ▼
finally: traj.persist()（事件增量落 SQLite runs + events 表）
```

### 2.3 上下文压缩链路（每轮进入 LLM 前）

```
messages
  │
  ▼ Layer 1  stale_snip（总是执行）
  │   同路径后续读取覆盖旧读取 → 旧 ToolResult 打 stale 标记替换为占位符（零成本）
  ▼ Layer 2  microcompact（利用率 > 50%）
  │   超长工具输出 → head+tail 摘要，保留原文指针（零成本）
  ▼ Layer 2.5 structured_snip（利用率 > 65% 且有轨迹事件）
  │   轨迹识别已完成 read→modify→verify 子任务 → 结构化摘要替换（零 LLM 成本）
  ▼ Layer 3  auto_compact（利用率 > 85%）
  │   LLM 摘要旧轮次 → [会话摘要] 注入；摘要文本同时蒸馏进记忆库
  ▼ Layer 4  truncate（仍超预算）
  │   保留 system + 首条 user + 尾部贪心 pair（原子性），超限消息丢弃
  ▼
发往 LLM
```

### 2.4 持久化 / 恢复链路

```
事件流（turn_start/llm_response/tool_call/tool_result/compression/
       permission_check/retry/supervision/error/run_end…）
  │
  ├─ persist() 增量写 SQLite：runs（概要）+ events（全量 JSON payload）
  ├─ export_jsonl() → JSONL 快照（eval 判题/审计用）
  │
  ├─ resume(run_id)：Trajectory.from_db → to_messages()（重放重建消息序列）
  │     ├─ 一致性校验（model/tools 集）→ 补执行 pending 工具 → 继续 _run_gen
  │     └─ chat_resume(run_id)：只恢复对话上下文，不重放工具
  └─ to_messages()：事件 → messages（含 auto_compact 摘要回放、tool_result 去重）
        └─ 供 LLM-as-Judge / TUI 轨迹重放 / 会话恢复
```

### 2.5 TUI 链路（异步 UI ↔ 同步 Agent）

```
Composer 输入
  │
  ▼
SessionManager（每会话独立 Agent 实例 + transcript）
  │
  ▼
Worker 线程跑 agent.chat()/resume()
  │
  ├─ StreamEvent ──→ call_from_thread ──→ _on_stream_event
  │                       ├─ TextDelta/ThinkingDelta → TuiTranscript entries → ConversationView 流式 Markdown（0.2s 节流）
  │                       ├─ ToolUseStart/ArgsDelta → 工具活动流（running→success/error）
  │                       └─ RetryNotice → system 行
  ├─ on_tool_result / on_state_change ──→ UI 更新（工具状态/压缩回收统计）
  ├─ on_permission ──→ 权限对话框（prewrite diff 预览）→ Decision/feedback 回传 Agent
  └─ on_run_complete → 回合结束 → 会话状态刷新
```

### 2.6 Eval 链路

```
eval/tasks.json（20 题）
  │
  ▼
build_matrix（基线全开 + 3 单变量关闭，OFAT / full 2×2×2）
  │
  ▼
run_eval 每 cell：
  ├─ _set_workdir（git clone + base_commit checkout + tar 缓存，按 cell 隔离）
  ├─ reset_workdir（git restore + clean -fdx 逐字节一致）
  ├─ ensure_env（uv venv + 依赖策展 + sitecustomize 桩模块注入）
  ├─ sanity_gate（F2P 必须断言失败 / P2P 必须通过，结果缓存）
  ├─ Agent.run(prompt, workdir) → Trajectory → _extract_stats（token/成本/压缩回收）
  ├─ verify_run（diff 非空 → 回滚测试文件 → apply test_patch → F2P/P2P pytest 实跑）
  ├─ run_metrics（read-before-edit 率 / edit→test 闭环 / 冗余工具调用）
  └─ TaskResult（verified + pass^k + 成本 + 失败分类）
  │
  ▼
generate_report → Markdown（pass^k 汇总 / 逐题胜负表 / 轨迹指标 / 监督质量 / 失败分布）
  │
  ▼ 离线
LLM-as-Judge（rubric 锚定评分 + 人工一致性审计）← to_messages() 重建轨迹
```

---

## 3. 优点

### 3.1 架构设计

1. **模块边界清晰、单一职责**。Agent Runtime / 工具 / 上下文 / 权限 / 记忆 / Repo Map / 模型抽象 / 轨迹 / 评测九大子系统相互解耦，`AGENTS.md` 为每个子系统定义了规范术语与"避免的别名"，README 架构图与实际代码一一对应。`src/agent/loop.py` 虽大，但对外只暴露 `Agent(config, backend).run/start/chat/resume` 编程接口，符合 ADR-0001"Agent 即库"决策。

2. **自定义 IR + Codec 抽象是亮点**（ADR-0002）。`ir.py` 的四种 Block（text/thinking/tool_use/tool_result）+ `StreamEvent` 十种事件类型语义照抄 Anthropic content block 模型；每个厂商一个薄 codec（`codecs/deepseek.py`、`codecs/anthropic.py`），上层业务代码零厂商分支。`encode_request`/`decode_response`/`StreamDecoder` 三段式设计，thinking 边界推断、tool_call index→id 映射、usage 归一化、角色交替修复都在 codec 内完成。`ToolSpec.to_openai_tool()/to_anthropic_tool()` 让工具声明同时适配两家 API。

3. **事件溯源轨迹（ADR-0003）**。每行一个类型化事件（含 turn/ts/usage），JSONL 落盘 + SQLite 增量持久化；`to_messages()` 可从事件流确定性重建消息序列（含 auto_compact 摘要回放、tool_result 去重），成为 TUI 重放、resume、LLM-as-Judge 的公共桥梁。事件模型比直接存 messages 多保留 token 用量、压缩明细、权限决策、监督评估等消息数组天然丢失的元数据——这是评测可追溯性的根基。

4. **统一流式事件协议 + Visitor 模式**（ADR-0005）。CLI（`RichStreamVisitor`）、TUI（`_on_stream_event`）、Eval（`_FakeBackend` 流）消费同一 `StreamEvent` 序列；`dispatch_event` + `NullVisitor` 让消费方只需覆盖关心的子集。`_StreamAggregator` 将 delta 事件聚合为完整 `ModelResponse`，聚合逻辑与传输解耦。

### 3.2 上下文工程

5. **五层压缩流水线是学术级的认真**（ADR-0011/0017）。`stale_snip`（同路径后续读取覆盖旧读取 → 占位符，零成本）→ `microcompact`（head+tail 摘要）→ `structured_snip`（用轨迹事件识别已完成 read→modify→verify 子任务，**零 LLM 成本**的结构化替换）→ `auto_compact`（LLM 全量摘要）→ `truncate`（硬截断兜底，保持 tool pair 原子性）。按"精准度"排序而非"紧急度"，每层有独立阈值与 `LayerReport` 明细，压缩事件全部落轨迹可审计。

6. **Token 计数有实证校准**。`context_tokens.py` 首选 DeepSeek 官方离线 tokenizer（偏差 <1%），并显式补偿 wire 层开销（`_WIRE_ENVELOPE_TOKENS=11`、`_WIRE_TOOL_RESULT_EXTRA=10`、`_WIRE_TOOL_CALL_STRUCT=40`），注释给出实测校准样本（本地 111,201 vs API 121,506）。`per_message_tokens` 缓存修复了 truncate 的 O(P×C) 性能问题。

7. **诚实面对权衡**。README 主动披露压缩改写历史导致 KV Cache 命中率从 93% 降至 27%、单 token 成本升 4 倍、净成本反升的矛盾，并基于 87 runs 数据承认五层流水线后半段在短任务集零触发——这种"自曝短处"的工程态度罕见且可贵。

### 3.3 工具与并发

8. **冲突可串行化并发调度有理论基础**（ADR-0012）。`concurrency.py` 为每个工具调用提取 `ResourceScope`（path + scope_type + op_type），读读并发、写写串行、WORKSPACE 级全串行；Windows 路径大小写归一化（`os.name == "nt"` 时 lowercase）、glob 前缀提取、超时后 `shutdown(wait=False)` 立即返回、上游失败级联取消——细节处理到位。

9. **Windows 平台适配是深度工程**。`bash` 工具自动 `chcp 65001`、注入 `PYTHONUTF8/PYTHONUNBUFFERED`、检测 cmd.exe 交互确认提示并给出非交互改写指引、多行 `python -c` 自动重写为临时脚本（cmd.exe 换行破坏 bug）、超时用 `taskkill /F /T` 杀进程树。这些是只在真实 Windows 环境踩坑才能积累的经验，README 工具描述也明确告知模型 cmd 语法差异。

10. **测试输出结构化信号**。`bash` 工具对 pytest 类命令追加 `[test] PASS (3 passed)` / `[test] FAIL (1 failed)` 结构化行，让模型获得明确的验证反馈信号——这是"目标驱动执行"原则的落地（与 `eval/metrics.py` 的 TEST_KEYWORDS 同源但产品层独立，不依赖 eval 层）。

### 3.4 权限与安全

11. **权限模型设计合理**（ADR-0013）。4 种模式（safe/normal/autoedit/auto）按**操作可逆性**切分信任等级（read/write/bash_safe/bash_dangerous/network 五类）；三层规则（持久/会话/单次）deny 优先级最高；`evaluate()` 是纯函数（便于测试）；每次决策落 `permission_check` 审计事件。持久化规则对非法正则优雅降级为字面匹配（`_rule_matches`），"一条坏规则不破坏整个权限系统"。

12. **写入前 diff 审查 + 拒绝理由反馈闭环**。`prewrite.py` 对 write_file/patch 计算带行数的 unified diff（`Operation.review`），TUI 权限对话框展示红删绿增；拒绝时用户可填理由，理由通过 `Operation.feedback` 回传给模型——形成"用户为什么拒绝"的反馈闭环，这是大多数 coding agent 缺失的能力。

13. **防御式编程贯穿始终**。Repo map 构建失败降级、记忆库初始化失败降级（warnings 而非崩溃）、压缩异常 → truncate 兜底、`stream_options.include_usage` 缺失等待、流式断连（`StreamDisconnect`）可重试分类……每一条降级路径都有对应测试。

### 3.5 评测体系（本项目最突出的优点）

14. **真验收而非假数字**。`verify.py` 的 sanity gate 双检（F2P 干净 checkout 必须断言失败、P2P 必须通过，结果按 `(repo, commit, deps-fingerprint)` 缓存）杜绝了"环境没搭好导致假失败/假成功"；`verify_run` 先回滚测试文件再 apply test_patch，防止 Agent 篡改测试文件钻空子（P0-3）；`metrics.diff_touches_test_files` 额外检测 conftest.py/pytest.ini 等配置文件触碰。README 明确废弃早期基于假 pass/fail 的 83%/93% 数字。

15. **pass^k 可靠性度量**（τ-bench 风格）：同一 (任务, 配置) k 次重复全部 verified 才计过，度量"可靠性"而非"运气"；逐题胜负表（head-to-head）诚实呈现"开过关不过/关过开不过"，承认总通过率差可能是 2 题噪声。

16. **成本工程化**：cache 命中按 1/4 单价核算（基于实测命中率校准）、全局成本熔断（`--max-cost`）、断点续跑 manifest（done 跳过 / failed 最多重试 2 次）、每 run 独立 SQLite db、venv 依赖指纹进元数据（可复现性）、repo tar 缓存免重复 clone（480 runs 场景）、workdir 按 cell 隔离（U5 修复）。

17. **多维度判题**：P0 真验收判 outcome，P0.5 `metrics.py` 判过程（read-before-edit 率、edit→test 闭环、冗余读取），P1 LLM-as-Judge 锚定 rubric 评分 + 人工一致性审计（`consistency()`），P2 八类失败分类（含"钻空子型伪完成""停滞"）。`classify.py` 是确定性规则而非另一个 LLM，可复现。

### 3.6 工程纪律

18. **测试质量高**：759 个测试覆盖压缩链（阈值触发顺序）、并发调度（冲突分组）、codec 双向转换（golden transcript）、TUI（74 个 widget/视图测试）、权限矩阵、eval 验证（21 个 harness 用例）；`_FakeBackend` 模式让 eval 框架冒烟不耗 API。

19. **文档体系完备**：20 份 ADR 记录"背景→备选方案→决策→后果"，18 份 plans 逐步骤实现，25 篇文章含教程（T1-T4），handoff 交接记录数据与结论（8/10 pass^3、消融无显著差异），`known-issues.md` 诚实跟踪未修复问题（U1-U4 + 修复记录）——这是个人项目中极罕见的文档成熟度。

20. **监督 Agent 设计克制**（ADR-0020）：观察者只"看与说"不碰工具；stuck 判停要求"连续两次 stuck 且期间零编辑"；`enabled=False` 默认关闭（未充分评测前不进默认路径）；每次监督调用（含失败）落审计事件；成本独立核算。功能默认关闭本身就是工程判断力。

---

## 4. 缺点与风险

### 4.1 严重问题

**S1 — CLI 模式默认配置下无法编辑文件（可用性缺陷）**

`src/cli/__init__.py` 不暴露权限模式参数，`AgentConfig.permission_mode` 默认 `"normal"`（`config.py`）。normal 模式下 write/patch/bash 均为 CONFIRM；而 CLI 未设置 `_on_permission`（`loop.py:_check_tool_permission` 中 `decision == CONFIRM and not self._on_permission → decision = DENY`），且 CLI 也不加载 `.agent/permission-rules.json`。**后果：`python -m src.cli "Fix the bug in stats.py"` 在默认配置下模型只能 read/glob/grep，任何写操作与 bash 全部被拒**，与 README 宣传的主用法矛盾。TUI 有 `/mode` 命令、eval 用 `permission_mode="auto"`，唯独 CLI 是死路。要么 CLI 增加 `--mode` 参数，要么加载规则文件，要么为无人值守模式设计 CONFIRM→ALLOW 的策略。

**S2 — 记忆系统与文档不符，且实际近乎未用**

README 技术栈表声称"SQLite + FTS5 (BM25)"，CHANGELOG v0.1.0 也写"SQLite FTS5 unified store"；但 `memory.py` 实际**只建了普通索引**（`idx_memories_kind`），检索是 `LIKE %term%` + 热度衰减排序——**没有任何 FTS5/BM25 代码**。文档与实现不符。更实质的问题：记忆写入依赖 auto_compact 蒸馏，而消融数据（87 runs）显示 auto_compact 在短任务**零触发**，所以记忆系统在真实评测中从未被使用；eval 里 `MemoryConfig(enabled=False)`。记忆是目前证据最薄弱的子系统。

**S3 — CLI 对 Anthropic 的接入是"伪接入"**

`src/cli/__init__.py` 中 `--provider anthropic` 实际把 base_url 写死为 `https://api.deepseek.com/anthropic`（DeepSeek 的 Anthropic 兼容端点），模型默认名 `claude-sonnet-4-5`。用户无法真正连接 Anthropic 官方 API（backend.py 的 `AnthropicBackend` 支持任意 base_url，但 CLI 层不暴露）。README 宣称"Powered by DeepSeek V4 Flash (or any OpenAI/Anthropic compatible backend)"，实际 Anthropic 官方路径不可用。

### 4.2 中等风险

**M1 — 巨型类违反单一职责**

`loop.py`（约 53K 字符）：ReAct 主循环、chat 会话、chat_resume、compact_chat、summarize、resume、监督 Agent（prompt 构造、过程信号统计、diff stat、transcript 转写）、权限预检、checkpoint、guidance 全部堆在 `Agent` 类里。`tui/app.py`（约 58K 字符）同理：事件分派、会话管理、picker、输入历史、权限对话框、轨迹重放、welcome 粒子动画全在一个类。`runner.py` 直接读写 `agent._on_permission`、`agent._chat_messages` 等私有属性，说明 Agent 的公共 API 设计不完整（回调应通过构造参数/公开 setter 注入）。巨型类使测试需大量 fixture 构造、任何修改都有回归风险。

**M2 — 上下文压缩与 KV Cache 的内生张力未解决**

README 自曝：压缩改写历史消息 → 缓存命中率 93%→27% → 单 token 成本 ×4 → 净成本反升（8 题消融：基线全开 $1.35 vs 关压缩 $0.78）。这意味着当前压缩策略在**成本维度上是负收益**，且五层流水线后半段（microcompact/structured_snip/auto_compact/truncate）在 87 runs 短任务中**全部零触发**——大量精心实现的代码没有任何实证收益。设计上需要一个"压缩收益 vs cache 损失"的联合决策模型（例如只在 cache 命中率低或窗口将满时才压缩），或至少按任务长度分层启用。

**M3 — resume 的已知数据一致性缺陷**

`known-issues.md` 自录：U2 resume 产生重复 compression 事件（需分析侧去重）；U4 resume × 并发时整组 pending 工具重跑（patch 会因 old_str 已被替换报错、bash 有重复副作用风险）——后者是真实的数据一致性风险，被标注"等待设计"而未修。`U3` 则承认 `loop.py:505` 的 `run_end(max_turns)` 是正常流程不可达的死代码。

**M4 — 记忆/工具注入缺乏检索时机控制**

`memory_search` 工具仅在 `_memory_store` 初始化成功且 task 非空时注册，`code_search` 仅在 repo index 非空时注册——但这两个工具的**注册时机在 `_init_run` 内一次性决定**，运行中 repo index 过期后 `code_search` 仍可用旧索引（`refresh()` 存在但无人调用），索引会随 Agent 编辑漂移。

**M5 — 危险命令分类的覆盖盲区**

`permission.py` 的 `_DANGEROUS_PATTERNS` 覆盖 rm/dd/chmod/管道 curl|sh/python -c 等，但**未覆盖** `git reset --hard`、`git clean -fdx`、`git checkout -- .`、`format`、`> 重定向写文件`（写文件本身走 write_file 工具，但 bash 里 `echo x > file` 不判危险）、`pip install`（网络）、`taskkill` 等。safe 模式（`bash_dangerous: DENY`）下这些命令可直通。同时 `^\s*python\s+-c\b` 判危险但 `python script.py` 不判——同能力不同分类。

**M6 — 工具输出截断按字符而非 token**

`_truncate_tool_content` 固定 50,000 字符（约 12-15K token），在 64K 窗口模型（deepseek-v4-pro）上单条工具结果即可吃掉近 1/4 预算；且截断阈值与上下文预算（`context_tokens.compute_budget`）无联动。grep 结果上限 500 条、glob 1000 条均为固定值，无 token 感知。

### 4.3 轻微问题 / 一致性

**L1 — 文档数字过时**：README 称"586 条自动化测试""516 tests"，实测 759 个；README 称"31 题任务集"，`eval/tasks.json` 实际 20 题（31 是含不可跑题的官方选择口径，README 有"20 题本机可跑"但结构图仍写 31）。

**L2 — 配置漂移**：`AgentConfig.max_turns=500` 但 eval 用 25/40（CHANGELOG 里 25→40）；`CONTEXT_WINDOWS` 硬编码 4 个模型，未知模型回退 64K；`create_anthropic_backend` 的 `base_url` 参数在 CLI 层被忽略（写死 DeepSeek 端点）；模型名 `deepseek-v4-flash`/`deepseek-v4-pro`/`claude-sonnet-4-5` 是否真实存在未验证。

**L3 — 私用 API 泛滥**：`agent._on_permission`、`agent._chat_messages`、`agent._chat_traj`、`agent._chat_bound_tools`、`backend._judge_model` 等被 runner/TUI/eval 直接读写；`_run_gen` 的 `turn_box: list[int]` 用可变列表传引用计数，属 hack。

**L4 — 静默降级过多**：memory/repo_map/compression/rules 的初始化失败均 `warnings.warn` 后继续，无结构化告警渠道；`_safe_read` 遇 UnicodeDecodeError 返回 None 静默跳过规则文件；`trajectory.persist` 的 checkpoint 失败只 warn——用户可能完全不知道记忆/索引/压缩已失效。

**L5 — 部分死代码与文档误导**：`loop.py:505` 兜底不可达（U3）；`articles/03` 教程对 max_turns 终止条件的描述误导（U3 标注）；`_GIT` 无 timeout；`eval/harness.py` 的 `--ablation-tasks` 分支不做 fake 模式 `tasks[:1]` 截断（小不一致）。

**L6 — 记忆系统 LIKE 检索的转义只处理了三个字符**（`\\`、`%`、`_`），`ESCAPE '\'` 声明了但查询参数未全部转义完整路径；`search()` 的排序 SQL 中 `julianday('now') - julianday(last_used_at)` 对非 UTC 存储可能算错时差（`_now()` 用 `datetime.now(timezone.utc)`，SQLite julianday 按 UTC 解析，尚一致，但脆弱）。

**L7 — eval fake 模式覆盖不足**：`_FakeBackend` 只覆盖 1 任务 × 1 cell，且 `stream()` 返回完整 end_turn 流（特意规避空流路径）；fake 冒烟无法触达并发/压缩/监督的真实分支。

**L8 — 中文注释与代码混排**：大量中文注释（工程可接受，面向中文用户），但部分注释是流水账式（如 `# Step 1 — Thinking 边界` 后跟详细过程），未来维护者（非中文）可读性受损；代码中硬编码中文提示语（错误消息、`[已过期:...]` 占位符），这些文本会直接进入 LLM 上下文，无 i18n 抽象。

**L9 — git 提交信息笔误**：`99be8cb` 提交信息 `"78 runs, .08"` 缺少 `$`（应为 $25.08），小但反映提交信息未校对。

**L10 — 性能隐患**：`repomap.refresh()` 每次变更后重读**所有**文件重新计数引用（O(全仓) 而非 O(变更)）；grep 用 Python `re` 而非 ripgrep；tree-sitter 仅支持 Python（`languages` 配置字段存在但未用）；`Agent._supervision_diff_stat` 每次监督调用跑一次 `git diff --stat`。

---

## 5. 改进建议（按优先级）

| 优先级 | 建议 | 对应问题 |
|---|---|---|
| P0 | CLI 增加 `--mode` 参数（或加载权限规则文件），让 `python -m src.cli "Fix..."` 真正可写 | S1 |
| P0 | 记忆系统二选一：实现 FTS5 或删除文档中的 FTS5 宣称；设计"摘要文本过长/有价值才蒸馏"的策略并接入长任务验证 | S2 |
| P0 | CLI `--provider anthropic` 暴露真实 base_url，或改文案为"DeepSeek Anthropic 兼容端点" | S3 |
| P1 | 拆分 `loop.py`/`app.py`：监督逻辑提取为独立模块；Agent 回调改为构造参数或公开 setter；`turn_box` 改状态对象 | M1 |
| P1 | 压缩层加入 cache 感知：命中率高时跳过改写历史的层，仅做 append-only 压缩；给五层流水线设计长任务评测验证收益 | M2 |
| P1 | 修 U4（工具执行前落 in-flight 标记，resume 识别中断组）；删除 U3 死代码并修正教程文档 | M3 |
| P1 | 为 `permission.py` 补充 `git reset --hard`/`git clean -fdx`/`> 重定向`/`pip install` 等危险模式并加测试 | M5 |
| P2 | 工具输出截断改为 token 感知并与预算联动；grep/glob 上限按预算缩放 | M6 |
| P2 | 代码 search 运行中增量 refresh（mtime 已实现，接入工具调用点） | M4 |
| P2 | 静默降级统一为结构化告警（回调/事件），TUI 可见"记忆索引失效" | L4 |
| P2 | 更新 README/CHANGELOG 中的测试数（759）、任务数（20）等过时数字 | L1 |
| P3 | 提交信息校对；`_GIT` 加 timeout；i18n 抽象（如需） | L8/L9/L5 |

---

## 6. 总结

**定位**：这是一个**架构成熟度远超市面大多数同类个人项目**的 Coding Agent 实现。它没有停留在"调 API 的壳"，而是把 Agent 产品的每个关键子系统（模型抽象、工具并发、上下文治理、权限安全、轨迹审计、评测闭环）都做了工程化设计，并用 ADR/plans/known-issues 记录决策与代价。

**最强项**：评测体系（真验收 + sanity gate + pass^k + 成本核算 + 失败分类）和上下文工程（五层压缩 + token 校准 + 轨迹驱动结构化压缩）是简历级亮点；759 测试全绿 + 文档完备度（20 ADR/18 plans/25 articles）证明工程纪律。

**最大短板**：CLI 默认配置不可写（S1）与记忆系统名不副实（S2）是产品可用性的硬伤；巨型类与私用 API 是长期可维护性隐患；压缩与 KV Cache 的张力、五层流水线后半段零实证收益，说明"设计复杂度"已超过"被验证的需求"——部分子系统存在**过度设计的嫌疑**（AGENTS.md 自己写的 Karpathy 原则第 2 条"Simplicity First"在压缩流水线/记忆系统上没有被完全遵守）。

**总体评级**：架构设计 B+ → A，工程纪律 A，产品可用性 C+，评测严谨度 A+。核心价值在"可复现的评测方法论"与"自研 Runtime 的完整闭环"，核心风险在"功能面广但证据面窄"——建议下一步用长任务（40+ 轮）验证压缩流水线后半段的真实收益，并补齐 CLI 可用性，而不是继续横向扩展子系统。

---

*审查完成时间：2026-08（基于 `git log` 最新提交 99be8cb）*
*测试基线：`pytest -q --ignore=tests/_target_bug` → 759 passed / 161.87s*
