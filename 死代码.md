# XClaw 死代码审查报告

> **2026-08-10 更新（ADR-0030 执行后）**：A 类 15 处 + 新增 `LayerReport.unit` + `SessionSidebar.set_current`
> 已全部删除（连同 3 处连带测试），vulture 复扫确认清零；B/C/D 类维持原结论不变。
> 以下为原始审查报告，供历史追溯。

> 审查对象：`src/`（产品代码，9,317 行）+ `eval/`（评测工具链，~4,000 行）
> 审查方法：**vulture 2.16 静态扫描**（min-confidence 60）+ **全量符号引用计数**（src+tests+eval 交叉验证）+ **逐项人工核实**（含 Textual 框架反射调用排除）
> 审查原则：只读分析，**不修改任何代码**
> 测试基线：759 passed（确认扫描结论不破坏现有行为）

---

## 0. 结论速览

| 分类 | 数量 | 说明 |
|---|---|---|
| **A. 完全死代码**（零引用） | 15 处 | 建议删除，无任何调用点（含测试） |
| **B. 仅测试使用**（生产零调用） | 6 处 | 删除则需连带删测试；保留则需补生产调用点 |
| **C. 防御性/有意保留** | 2 处 | known-issues 已记录，不建议动 |
| **D. vulture 误报**（实际非死代码） | ~30 处 | Textual 框架回调/反射调用等 |

总体判断：**死代码规模可控、无危害性**。无一处会造成资源泄漏或安全风险；最值得关注的是 B 类中 `XClawAgentRunner.run_chat/chat_resume/end_chat`（功能完整但生产路径未接线）与记忆/索引层的"仅测试"方法（印证了 `审查.md` 中"记忆系统实际未用"的结论）。

---

## 1. A 类：完全死代码（零引用，src+tests+eval 均无调用）

### 1.1 配置字段（3 处）——定义后从未被读取

| 位置 | 名称 | 证据 |
|---|---|---|
| `src/agent/config.py:63` | `MemoryConfig.search_top_k` | 全项目无 `search_top_k` 读取；`memory_tool.py` 的 handler 硬编码 `k=5` |
| `src/agent/config.py:72` | `RepoMapConfig.languages` | 全项目无读取；`loop.py` 构造 `RepoIndex` 时不传 |
| `src/agent/repomap.py:40` | `RepoIndex.languages` | dataclass 字段从未使用——`build()` 无条件 `rglob("*.py")`，不看 `languages`（印证"tree-sitter 仅支持 Python"的现状） |

> 注：`RepoMapConfig.languages` 与 `RepoIndex.languages` 是同一死配置的两层传递，删任意一层都会让另一层变半死，需联动处理。

### 1.2 类属性 / 实例属性（4 处）——写而不读或零引用

| 位置 | 名称 | 证据 |
|---|---|---|
| `src/agent/loop.py:63` | `_StreamAggregator._result` | `__init__` 初始化后**从不赋值也不读取**；`result()` 返回新构造的 ModelResponse，`self._result` 纯冗余 |
| `src/agent/memory.py:10` | `MemoryStore._db_path` | 赋值后从不读取（后续用的是局部变量 `db_path`） |
| `src/tui/app.py:123` | `XClawApp._pending_guidance` | 初始化后从不使用——真正的 guidance 队列在 `SessionState.pending_guidance`（`_add_guidance/_drain_guidance` 用的是 state 字段） |
| `src/tui/widgets/conversation.py:23` | `ConversationView._pinned` | 赋值 `True` 后从不读取（疑似早期 scroll 固定逻辑残留） |

### 1.3 方法 / 函数（3 处）——定义后零调用

| 位置 | 名称 | 证据 |
|---|---|---|
| `src/agent/memory.py:88` | `MemoryStore.recent()` | 生产代码零调用；`memory_tool.py` 只用 `search()` |
| `src/tui/widgets/sidebar.py:115` | `SessionSidebar.set_current()` | 零调用——`update_sessions(items, current_run_id)` 直接传参设置 `_current_run_id` |
| `eval/metrics.py:186` | `load_gold()` | 零调用；`eval/judge.py:11` 的 `from eval.metrics import load_gold  # noqa: F401 (保持 gold 维度可扩展)` 只 import 不用 |

### 1.4 函数参数（2 处）——形参从未使用

| 位置 | 名称 | 证据 |
|---|---|---|
| `src/agent/memory.py:39` | `ingest(confidence=...)` | 函数体内未用——INSERT 语句**不写 confidence 列**（列默认 1.0）；唯一调用点 `loop.py:558` 也不传该参数 |
| `src/tui/app.py:495` | `_begin_new_session(first_text)` | 函数体内未用；调用点 `_submit_chat(text)` 传了实参但被忽略 |

### 1.5 枚举成员（2 处）——定义后无任何 emit/消费

| 位置 | 名称 | 证据 |
|---|---|---|
| `src/agent/trajectory.py:34` | `EventType.retry_divergence` | 全项目（src+tests+eval）仅定义处出现，无 emit 无消费 |
| `src/agent/trajectory.py:37` | `EventType.mode_change` | 同上 |

> 这两枚成员疑似早期设计（retry 分歧检测 / 权限模式切换事件）未落地，`to_messages()` 等消费方也无对应分支。

### 1.6 Eval 层数据字段（3 处）——写而不读

| 位置 | 名称 | 证据 |
|---|---|---|
| `eval/judge.py:38` | `JudgeResult.raw_output` | 被 `judge_run()` 赋值、`_load_judge_results()` 赋值 `""`，但**从不读取**——`to_dict()` 显式跳过它 |
| `eval/env.py:135` | `EnvSpec.repo_key` | 构造时传入但从不读取 |
| `eval/select_tasks.py:11` | `select_tasks(output_dir=...)` | 形参未使用（函数写 `output_path`，不写 `output_dir`） |

### 1.7 临时 fake 类内部（eval/harness.py `_FakeBackend`）

- `call_count`：只自增从不读取（fake 冒烟不校验）；
- `complete()/stream()` 的 `tools` 参数：100% 未使用（vulture 全置信度）。
- 影响面：仅 `--fake` 冒烟模式，非产品路径。

---

## 2. B 类：仅测试使用（生产代码零调用，但有测试覆盖）

> 这类"死代码"严格说是**生产死、测试活**。删除需连带删测试；保留则建议补生产调用点或删除。它们恰好都是"功能实现了但没接线"的典型案例。

| 位置 | 名称 | 生产调用 | 测试覆盖 |
|---|---|---|---|
| `src/agent/loop.py:352` | `Agent.in_chat` property | 零（CLI/TUI/runner 均不读） | `tests/test_chat_session.py` 3 处断言 |
| `src/agent/retry.py:48` | `RetryDecision.terminal_reason` | 零（`loop.py` 只读 `retryable/error_kind/reason`） | `tests/test_retry.py` 16 处断言 |
| `src/tui/runner.py:59` | `XClawAgentRunner.run_chat()` | 零——`app.py` 的 chat 路径直接 `agent.chat()`（`_run_agent_worker`），不走 runner | `tests/tui/test_runner.py` 7 个测试 |
| `src/tui/runner.py:71` | `XClawAgentRunner.chat_resume()` | 零——`app.py:980` 直接 `agent.chat_resume()` | `tests/tui/test_runner.py:166` |
| `src/tui/runner.py:77` | `XClawAgentRunner.end_chat()` | 零——同上 | `tests/tui/test_runner.py:180` |
| `src/tui/state.py:57` | `TuiTranscript.active_tool` | 零读取——`record_tool_activity()` 只写；UI activity 行走 `_record_tool_activity` 动画逻辑，不消费该字段 | `tests/tui/test_mixin.py` + `test_state.py` 5 处断言 |

> **重点**：`XClawAgentRunner` 的 docstring 声称"app must install callbacks before calling `run_chat`"，但生产代码只用 `resume()`。runner 的三个 chat 方法（共约 60 行）是**完整实现但未接线的双轨架构残留**——TUI 实际走的是 `_run_agent_worker`/`_run_resume_worker` 直连 Agent 的另一条轨道。
>
> `active_tool` 与 `Agent.in_chat`、`terminal_reason` 同属"为 API 完整性而写、无生产消费方"的字段，测试把它们当 API 契约钉住了。

---

## 3. C 类：防御性 / 有意保留（不建议修改）

| 位置 | 说明 | 依据 |
|---|---|---|
| `src/agent/loop.py` while 循环后 `run_end(max_turns)` | 正常流程**不可达**（`turn+1 >= max_turns` 熔断在 tool_use 分支内先 return），但被 `docs/known-issues.md` U3 记录为"最后防线"防御性代码 | U3 已明确"不改代码，文档标记" |
| `eval/judge.py:11` `from eval.metrics import load_gold  # noqa: F401` | 注释明示"保持 gold 维度可扩展"，是有意保留的死 import | 与 1.3 的 `load_gold()` 呼应 |

---

## 4. D 类：vulture 误报排除（实际非死代码）

以下条目 vulture 报告为 unused，经核实**均为框架反射调用或动态访问**，不构成死代码：

| 类别 | 条目 |
|---|---|
| Textual 框架回调 | `XClawApp.compose/on_mount/on_unmount/on_key/get_default_screen/on_composer_text_area_submitted/on_session_sidebar_session_selected/on_session_sidebar_session_delete_requested`；`SessionSidebar.compose/on_click/can_focus`；`screens/confirm.py`、`screens/permission.py` 的 `on_mount/on_button_pressed/on_key` |
| Textual 框架属性 | `XClawApp.CSS_PATH/ALLOW_SELECT/BINDINGS`；`widgets/common.py:ALLOW_SELECT`（框架读取用于渲染） |
| Textual action（BINDINGS 绑定） | `action_toggle_thinking`（t 键）、`action_show_help`（F1）、`action_copy_or_interrupt`（Ctrl+C）、`action_toggle_sidebar`（Ctrl+B） |
| TYPE_CHECKING stub | `mixin.py:59` `query_one(expect_type=...)`——仅类型标注用途，属设计意图 |
| 动态访问 | `metrics.py` `RunMetrics` 字段（`to_dict()` 用 `self.__dict__` 序列化）；`app.py:746` `cursor_location`（Textual 控件 setter）；`_on_terminal_resized`（被 `widgets/common.py:114` `getattr` 动态调用） |
| 内部 helper（有调用） | `tools.py` 的 `_rewrite_multiline_python`/`_looks_like_interactive_confirm`/`_interactive_confirm_guidance`；`prewrite.py` 的 `_proposed_text`/`_unified_diff`；`trajectory.py` 的 `_decode_block`/`from_events` |

---

## 5. 关联发现（边界情况）

### 5.1 `ir.py` 四个 `_type()` 方法——src 死、eval 活

`TextBlock._type()` / `ThinkingBlock._type()` / `ToolUseBlock._type()` / `ToolResultBlock._type()` 四个 staticmethod：
- **src 生产代码零调用**（各 Block 的 `to_dict()` 硬编码 type 字符串）；
- **唯一调用点在 `eval/judge.py:121`**：`t = b._type() if hasattr(b, "_type") else "block"`。

属于"半死代码"：移除需同步改 judge.py 的 `_transcript_text`（改为 `isinstance` 分派或读 `to_dict()`）。顺带 `BlockType` 类型别名（`ir.py:8`）仅被这四个 `_type()` 的返回注解使用，随其处置。

### 5.2 与 `审查.md` 的交叉印证

- B 类中 `MemoryStore.recent()`、`RepoIndex.refresh()` 虽被测试覆盖，但**生产路径零调用**——印证了 `审查.md` 中"记忆系统实际未被使用""repo index 过期后 refresh 无人调用"的观察；
- `MemoryConfig.search_top_k` 死字段印证"记忆配置项大多未接线"；
- `XClawAgentRunner` 双轨残留印证"TUI 与核心耦合度"问题（app 直连 Agent 私用属性 `_on_permission` 等，绕过了 runner 这一层抽象）。

---

## 6. 统计与建议

```
完全死代码（A 类）:   15 处  ← 建议直接删除（无测试依赖，删除安全）
仅测试使用（B 类）:    6 处  ← 二选一：接线或删（连带删测试）
防御性保留（C 类）:    2 处  ← 不动（文档已标记）
误报排除（D 类）:    ~30 处  ← 不动
```

### 建议删除顺序（按价值/风险比）

1. **A-1.5 枚举成员**（`retry_divergence`/`mode_change`）：零风险，删除即清；
2. **A-1.2/1.3 属性与方法**（`_result`/`_db_path`/`_pending_guidance`/`_pinned`/`set_current`/`recent`/`load_gold`）：零风险，但 `load_gold` 需连同 judge.py 的 noqa import 一起删；
3. **A-1.4 死参数**（`confidence`/`first_text`）：需同步改调用点签名（`ingest()` 调用点、`_begin_new_session()` 调用点各 1 处）；
4. **A-1.1 配置字段**（`search_top_k`/两处 `languages`）：需连同 `to_public_dict()`/`from_db` 的反序列化路径一起评估（配置字段删除会影响已存轨迹的 config_json 兼容——`from_db` 已有 unknown-field 跳过逻辑，风险可控）；
5. **B 类 6 处**：先决策"接线 or 删除"。若删除，`test_runner.py` 的 7 个测试、`test_chat_session.py` 的 `in_chat` 断言、`test_retry.py` 的 `terminal_reason` 断言、`test_mixin/test_state` 的 `active_tool` 断言需同步裁剪；
6. **5.1 `_type()`**：删除需同时改 `eval/judge.py:121`（半死，非纯删）。

### 不建议动

- C 类 2 处（防御性/有意）；
- D 类全部（框架契约）。

---

*扫描工具：vulture 2.16（min-confidence 60）+ 自定义 AST 符号引用计数*
*核实范围：`src/` + `eval/` + `tests/` 全部 .py（测试 759 用例全绿，确认无引用遗漏）*
